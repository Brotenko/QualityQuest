namespace Assets.Scripts.Game
{
    /// <summary>
    /// Enum is used to specify the current location so the corresponding background and ambient sound can me loaded accordingly.
    /// </summary>
    public enum BackgroundType
    {
        University,
        Internship,
        Meeting,
        Party,
        Office,
        Beach
    }

}