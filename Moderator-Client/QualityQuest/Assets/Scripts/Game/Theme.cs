namespace Assets.Scripts.Game
{
    /// <summary>
    /// Enum is used to specify the current theme so the corresponding music can me loaded accordingly.
    /// </summary>
    public enum Theme
    {
        None,
        Normal,
        Party,
        Beach
    }
}
